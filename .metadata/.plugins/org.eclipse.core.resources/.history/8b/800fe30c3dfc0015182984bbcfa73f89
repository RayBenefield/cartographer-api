package com.cartographerapi.functions;

/**
 * Writer repository interface for PlayerGameCounts. This allows saving,
 * updating, and deleting a PlayerGameCountsObject in a data source.
 * 
 * @author GodlyPerfection
 *
 */
public interface PlayerGameCountsWriter {
	
	/**
	 * Save the given PlayerGameCounts object.
	 * 
	 * @param counts
	 * @return
	 */
	public PlayerGameCounts savePlayerGameCounts(PlayerGameCounts counts);

}
